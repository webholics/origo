; Origo SPARQL endpoint configuration

[database]

; MySQL database configuration
host = "localhost"
name = "[database name]"
username = "[username]"
password = "[password]"

[endpoint]

; This API key gives read and write access to the triple store.
key = "[api_key]"

[misc]

; Set to 1 or 0
; Should be disabled on production servers
display_errors = 0

